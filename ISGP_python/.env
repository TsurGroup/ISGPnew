# APP_ENV sets the current environment for the app.
# Supported values:
#   dev  – development mode (default for local work)
#   test – testing mode (use mock data, bypass cache/DB)
#   prod – production mode (use real cache, DB, etc.)
APP_ENV=dev